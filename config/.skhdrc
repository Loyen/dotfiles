## NOTE(koekeishiya): A list of all built-in modifier and literal keywords can
#                     be found at https://github.com/koekeishiya/skhd/issues/1
#
#                     A hotkey is written according to the following rules:
#
#                       hotkey   = <keysym> ':' <command> |
#                                  <keysym> '->' ':' <command>
#
#                       keysym   = <mod> '-' <key> | <key>
#
#                       mod      = 'built-in mod keyword' | <mod> '+' <mod>
#
#                       key      = <literal> | <keycode>
#
#                       literal  = 'single letter or built-in keyword'
#
#                       keycode  = 'apple keyboard kVK_<Key> values (0x3C)'
#
#                       ->       = keypress is not consumed by skhd
#
#                       command  = command is executed through '$SHELL -c' and
#                                  follows valid shell syntax. if the $SHELL environment
#                                  variable is not set, it will default to '/bin/bash'.
#                                  when bash is used, the ';' delimeter can be specified
#                                  to chain commands.
#
#                                  to allow a command to extend into multiple lines,
#                                  prepend '\' at the end of the previous line.
#
#                                  an EOL character signifies the end of the bind.

# focus window
cmd + ctrl + alt - h : yabai -m window --focus west
cmd + ctrl + alt - j : yabai -m window --focus south
cmd + ctrl + alt - k : yabai -m window --focus north
cmd + ctrl + alt - l : yabai -m window --focus east

# equalize size of windows
cmd + ctrl + alt + shift - 0 : chunkc tiling::desktop --equalize

# swap window
cmd + ctrl + alt + shift - a : yabai -m window --swap west
cmd + ctrl + alt + shift - s : yabai -m window --swap south
cmd + ctrl + alt + shift - w : yabai -m window --swap north
cmd + ctrl + alt + shift - d : yabai -m window --swap east

# move window
cmd + ctrl + alt + shift - h : yabai -m window --warp west
cmd + ctrl + alt + shift - j : yabai -m window --warp south
cmd + ctrl + alt + shift - k : yabai -m window --warp north
cmd + ctrl + alt + shift - l : yabai -m window --warp east

# focus space
cmd + alt - 1 : yabai -m space --focus 1
cmd + alt - 2 : yabai -m space --focus 2
cmd + alt - 3 : yabai -m space --focus 3
cmd + alt - 4 : yabai -m space --focus 4
cmd + alt - 5 : yabai -m space --focus 5
cmd + alt - 6 : yabai -m space --focus 6
cmd + alt - 7 : yabai -m space --focus 7
cmd + alt - 8 : yabai -m space --focus 8
cmd + alt - 9 : yabai -m space --focus 9
cmd + alt - 0 : yabai -m space --focus 10

# send window to space and focus space
cmd + alt + shift - 1 : yabay -m window --space 1; yabai -m space --focus 1
cmd + alt + shift - 2 : yabay -m window --space 2; yabai -m space --focus 2
cmd + alt + shift - 3 : yabay -m window --space 3; yabai -m space --focus 3
cmd + alt + shift - 4 : yabay -m window --space 4; yabai -m space --focus 4
cmd + alt + shift - 5 : yabay -m window --space 5; yabai -m space --focus 5
cmd + alt + shift - 6 : yabay -m window --space 6; yabai -m space --focus 6
cmd + alt + shift - 7 : yabay -m window --space 7; yabai -m space --focus 7
cmd + alt + shift - 8 : yabay -m window --space 8; yabai -m space --focus 8
cmd + alt + shift - 9 : yabay -m window --space 9; yabai -m space --focus 9
cmd + alt + shift - 0 : yabay -m window --space 10; yabai -m space --focus 10

# focus monitor
cmd + ctrl + alt - z  : yabai -m display --focus prev
cmd + ctrl + alt - c  : yabai -m display --focus next
cmd + ctrl + alt - 1  : yabai -m display --focus 1
cmd + ctrl + alt - 2  : yabai -m display --focus 2
cmd + ctrl + alt - 3  : yabai -m display --focus 3

# send window to monitor and follow focus
cmd + ctrl + alt + shift - z  : yabai -m window --display prev; yabai -m display --focus prev
cmd + ctrl + alt + shift - c  : yabai -m window --display next; yabai -m display --focus next
cmd + ctrl + alt + shift - 1  : yabai -m window --display 1; yabai -m display --focus 1
cmd + ctrl + alt + shift - 2  : yabai -m window --display 2; yabai -m display --focus 2
cmd + ctrl + alt + shift - 3  : yabai -m window --display 3; yabai -m display --focus 3

# increase window size
cmd + ctrl + alt - y : yabai -m window --resize left:-20:0
cmd + ctrl + alt - u : yabai -m window --resize bottom:0:20
cmd + ctrl + alt - i : yabai -m window --resize top:0:-20
cmd + ctrl + alt - o : yabai -m window --resize right:20:0

# decrease window size
cmd + ctrl + alt + shift - y : yabai -m window --resize left:20:0
cmd + ctrl + alt + shift - u : yabai -m window --resize bottom:0:-20
cmd + ctrl + alt + shift - i : yabai -m window --resize top:0:20
cmd + ctrl + alt + shift - o : yabai -m window --resize right:-20:0

# rotate tree
cmd + ctrl + alt - r : yabai -m space --rotate 90

# toggle window fullscreen
cmd + ctrl + alt - f : yabai -m window --toggle zoom-fullscreen

# toggle window native fullscreen
cmd + ctrl + shift + alt - f : yabai -m window --toggle native-fullscreen

# toggle window split type
cmd + ctrl + alt - e : yabai -m window --toggle split

# float / unfloat window
cmd + ctrl + alt - t : yabai -m window --toggle float

# toggle sticky, float and resize to picture-in-picture size
cmd + ctrl + alt - x : yabai -m window --toggle sticky;\
                       yabai -m window --grid 5:5:4:0:1:1

# change layout of desktop
cmd + ctrl + alt - a : yabai -m space --layout bsp
cmd + ctrl + alt - d : yabai -m space --layout float
